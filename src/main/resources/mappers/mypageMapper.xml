<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.mypage">
	<select id="updateUI" resultType="kr.co.domain.MemberVO">
		select * from member where mnum=#{mnum}
	</select>
	<update id="update">
		UPDATE member SET 
		pw = #{pw}, name = #{name}, email = #{email},address = #{address}
		updateDate = SYSDATE WHERE mnum = #{mnum}
	</update>
</mapper>